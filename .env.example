# Network Device Credentials
# Copy this file to .env and fill in your actual credentials
# DO NOT commit .env to version control

# Common credentials (used if device-specific not set)
NETWORK_USER=admin
NETWORK_PASSWORD=changeme

# FortiGate specific
FORTIGATE_HOST=192.168.1.99
FORTIGATE_USER=admin
FORTIGATE_PASSWORD=changeme

# Ansible Vault password (alternatively use vault_password.txt)
ANSIBLE_VAULT_PASSWORD=changeme

# Git remote for config backup (optional)
GIT_REMOTE_URL=
GIT_BRANCH=main
